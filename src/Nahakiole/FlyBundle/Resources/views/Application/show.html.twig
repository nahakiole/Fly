{% extends 'FlyBundle:Default:index.html.twig' %}

{% block header %}
    <div class="container">
        <div class="row">
    <div class="col-md-12">
    <h1> {{ entity.name }}
    </h1></div>
    </div>
    </div>
{% endblock %}
{% block content -%}







    <div class="media">
        <a class="pull-left"
           href="#">
            <img src="{{ asset('bundles/fly/assets/') }}{{ entity.icon }}"
                 alt="..."
                 height="128">
        </a>
        <div class="media-body">
            <p>{{ entity.description }}</p>
        </div>
    </div>
    <a class="btn btn-default"
       href="{{ path('application') }}">
        Back to the list
    </a>


{% endblock %}
